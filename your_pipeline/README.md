# Your Pipeline

## Run once (local)
1) Start the provided local API in `api/` (separate terminal).
2) Create `.env` from `.env.example` and set your values.
3) Run the flow:
```bash
python -m your_pipeline.scripts.run_flow
